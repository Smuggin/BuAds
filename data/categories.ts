/** Default product categories (built-ins are not removable). Mirrors prototype. */
import type { Category } from "./types";

export const DEFAULT_CATEGORIES: Category[] = [
  "Skincare",
  "Fashion",
  "Bundle",
  "Beauty",
  "Other",
];
